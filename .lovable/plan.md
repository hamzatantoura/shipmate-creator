النتيجة الدقيقة من الفحص:

- حساب شركة مسارات موجود فعلاً في نظام المصادقة بالبريد الداخلي:
  - اسم المستخدم الفعلي: `msarat1`
  - البريد الداخلي: `msarat1@courier.sila.local`
  - الحساب مؤكد، غير محظور، وله كلمة مرور، ومربوط بدور `vendor`.
- الربط صحيح أيضاً:
  - جدول `couriers` لشركة `masarat` مربوط بنفس المستخدم `af176cc4-...`
  - جدول `profiles` فيه الدور `vendor`
  - جدول `user_roles` فيه الدور `vendor`
- آخر دخول ناجح لهذا الحساب كان بتاريخ 2026-05-17، لذلك الحساب كان يعمل سابقاً.
- سجلات الدخول الحالية لا تظهر أي محاولة فاشلة باسم `msarat1`، وهذا يعني أن المحاولة التي أعطت رسالة الخطأ غالباً لم تصل كدخول صحيح بهذا الحساب، أو أن الخطأ ظهر بعد تغيير/إدخال كلمة مرور غير مطابقة.

سبب المشكلة الأرجح في التطبيق:

- صفحة الإدارة تعرض وتنسخ اسم المستخدم فقط، لكنها لا تتحقق عملياً من كلمة المرور القديمة لأن كلمات المرور لا تُخزن ولا يمكن قراءتها.
- عند إعادة تعيين كلمة المرور من تبويب “الدخول”، الدالة تغيّر كلمة المرور فوراً، لكن لا يوجد اختبار دخول آلي بعدها يؤكد أن `msarat1 + كلمة المرور الجديدة` يعملان فعلاً.
- لذلك إذا تم إدخال/نسخ كلمة مرور مختلفة أو تغيّرت سابقاً، ستظهر رسالة “بيانات الدخول غير صحيحة” رغم أن اسم المستخدم صحيح والحساب والربط صحيحان.

الخطة المقترحة للإصلاح:

1. تعديل تبويب “الدخول” داخل ملف شركة الشحن:
   - بعد حفظ اسم المستخدم أو إعادة تعيين كلمة المرور، يتم عرض بيانات الدخول الحالية بشكل أوضح: `msarat1` فقط، وليس معرّف المستخدم الطويل.
   - إضافة تنبيه واضح أن كلمة المرور القديمة غير قابلة للعرض، ويجب استخدام آخر كلمة مرور تم تعيينها فقط.

2. إضافة فحص بعد إعادة تعيين كلمة المرور:
   - بعد استدعاء دالة `reset-courier-password` بنجاح، يقوم التطبيق بمحاولة تحقق آمنة من أن `msarat1@courier.sila.local` يقبل كلمة المرور الجديدة.
   - إذا نجح الفحص، تظهر رسالة: “تم التحقق من بيانات الدخول”.
   - إذا فشل، تظهر رسالة خطأ حقيقية بدل ترك الإدارة تعتقد أن البيانات صحيحة.

3. تحسين رسالة الخطأ في صفحة تسجيل الدخول لشركات الشحن:
   - إذا كتب المستخدم اسم مستخدم شركة شحن بدون `@` وفشل الدخول، تظهر رسالة أكثر تحديداً: “اسم المستخدم أو كلمة المرور غير صحيحين لحساب شركة الشحن”.
   - هذا لا يغيّر آلية الدخول ولا يفتح تسجيل ذاتي لشركات الشحن.

4. عدم تغيير قاعدة البيانات أو سياسة إنشاء حسابات الشحن:
   - إنشاء حسابات شركات الشحن سيبقى حصراً من الإدارة.
   - لن يتم إضافة تسجيل ذاتي ولا تغيير أدوار أو صلاحيات.

الملفات المتوقع تعديلها عند التنفيذ:

- `src/features/admin/components/AdminCouriersManagement.tsx`
- `src/features/auth/pages/Login.tsx`

لا توجد حاجة لتعديل بنية قاعدة البيانات.