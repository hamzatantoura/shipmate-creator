// Re-export to preserve legacy import paths after the notifications feature
// module was extracted into `src/features/notifications/`.
export { default } from "@/features/notifications/components/NotificationBell";